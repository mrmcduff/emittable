# emittable
A playground project for interview question testing and the node event emitter pattern

This is a simple node project that re-implements the node EventEmitter pattern. It is not intended to be novel, but instead to futher my own understanding of the core concepts.
